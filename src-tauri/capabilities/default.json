{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "overlay-*", "rec-toolbar", "rec-border", "cli-render"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "updater:default"
  ]
}

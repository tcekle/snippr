{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "overlay-*", "rec-toolbar"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
